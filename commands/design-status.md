---
description: Show the preview server status and list of projects
allowed-tools: mcp__herklaude-design__design_status, mcp__herklaude-design__design_list
---

Call both `design_status` and `design_list`.

Report:
- Whether the server is running or stopped. If running, include the URL.
- A list of all projects. If none exist, say so.
